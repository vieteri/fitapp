export const metadata = {
  title: "Workouts | FitApp",
  description: "Start a new workout or choose from your routines",
};

export default function WorkoutsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 